name: gobfuscate
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'gobfuscate version'
        required: true
        default: 'master'

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: ['amd64', 'arm64']
        go: ['1.17', '1.18']

    steps:
    - name: Use Go ${{matrix.go}}
      uses: actions/setup-go@v2
      with:
        go-version: ${{matrix.go}}

    - name: Build On ${{matrix.arch}} ${{matrix.go}}
      timeout-minutes: 5
      run: |
        GOOS="linux" \
        GOARCH="${{matrix.arch}}" \
        go install github.com/unixpickle/gobfuscate@"${{github.event.inputs.version}}"

        gopath=$(go env GOPATH)
        file="${gopath}/bin/gobfuscate"
        if [[ ${{matrix.arch}} = "arm64" ]]; then
          file="${gopath}/bin/linux_arm64/gobfuscate"
        fi
        cp ${file} ${{github.workspace}}/gobfuscate-linux-${{matrix.go}}-${{matrix.arch}}
      shell: bash

    - name: Cache files
      id: restore-build
      uses: actions/cache@v2
      with:
        path: ${{github.workspace}}/gobfuscate-linux-${{matrix.go}}-${{matrix.arch}}
        key: gobfuscate-linux-${{matrix.go}}-${{matrix.arch}}

  release:
    needs: [linux]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/cache@v2
      with:
        path: ${{github.workspace}}/gobfuscate-linux-1.17-amd64
        key: gobfuscate-linux-1.17-amd64
    - uses: actions/cache@v2
      with:
        path: ${{github.workspace}}/gobfuscate-linux-1.17-arm64
        key: gobfuscate-linux-1.17-arm64
    - uses: actions/cache@v2
      with:
        path: ${{github.workspace}}/gobfuscate-linux-1.18-amd64
        key: gobfuscate-linux-1.18-amd64
    - uses: actions/cache@v2
      with:
        path: ${{github.workspace}}/gobfuscate-linux-1.18-arm64
        key: gobfuscate-linux-1.18-arm64

    - name: Upload
      if: ${{ success() }}
      uses: actions/upload-artifact@v2
      with:
        name: gobfuscate
        path: ${{github.workspace}}/gobfuscate-linux-*
        retention-days: 1